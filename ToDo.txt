1. responsive page
2. Fix button size (< A >, A)
3. remove warnings
4. optimize css (scss)
5. d3.js v5
6. type control
7. publish app on github?

---
1. axes - same scale
2. errors
3. day/hour toggle

---
1. Factories points
2. Clicks on factories - rays to sensors
3. Circle transparency problem
4. Control point coordinates correctness

-

5. Wind diagram on the right
6. Sensor legend on the right

---

7. remove unnessessary code

Scatter + wind diagram for third view

-----------------------------------------

use d3 filter!

    svg.selectAll("circle")
        .data(data)
        .enter().append("circle")
        .filter(function(d) { return d.year == '2008' })
        .attr("class", "circle")
        .attr("r", 4)
        .attr("cx", function(d) { return x(d.year); })
        .attr("cy", function(d) { return y(d.avg); })
        .on("click", function(d) {
            commentBox.text( function() { return comment; })
            .style("opacity", 0.8)
            .style("left", (d3.event.pageX)-0 + "px")
            .style("top", (d3.event.pageY)-0 + "px");
        })
        .each(update);

